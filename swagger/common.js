//1. Update/Delete About
/**
 * @swagger
 *  components:
 *    schemas:
 *      AboutUpdateResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: about updated
 *      AboutDeleteResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: about deleted
 */

//2. Update/Delete Country
/**
 * @swagger
 *  components:
 *    schemas:
 *      CountryUpdateResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: country updated
 *      CountryDeleteResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: country deleted
 */

//3. Update/Delete City
/**
 * @swagger
 *  components:
 *    schemas:
 *      CityUpdateResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: city updated
 *      CityDeleteResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: city deleted
 */

//4. Update/Delete Contact
/**
 * @swagger
 *  components:
 *    schemas:
 *      ContactUpdateResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: contact updated
 *      ContactDeleteResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: contact deleted
 */

//5. Update/Delete Day
/**
 * @swagger
 *  components:
 *    schemas:
 *      DayUpdateResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: day updated
 *      DayDeleteResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: day deleted
 */

//6. Update/Delete Region
/**
 * @swagger
 *  components:
 *    schemas:
 *      RegionUpdateResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: region updated
 *      RegionDeleteResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: region deleted
 */

//7. Update/Delete Category
/**
 * @swagger
 *  components:
 *    schemas:
 *      CategoryUpdateResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: category updated
 *      CategoryDeleteResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: category deleted
 */

//8. Update/Delete Setting
/**
 * @swagger
 *  components:
 *    schemas:
 *      SettingUpdateResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: setting updated
 *      SettingDeleteResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: setting deleted
 */

//9. Update/Delete Template
/**
 * @swagger
 *  components:
 *    schemas:
 *      TemplateUpdateResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: template updated
 *      TemplateDeleteResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: template deleted
 */

//10. Update/Delete WebView
/**
 * @swagger
 *  components:
 *    schemas:
 *      WebViewUpdateResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: web-view updated
 *      WebViewDeleteResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: web-view deleted
 */

//11. Update/Delete Notification
/**
 * @swagger
 *  components:
 *    schemas:
 *      NotificationUpdateResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: notification updated
 *      NotificationDeleteResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: notification deleted
 */

//3. City
/**
 * @swagger
 *  components:
 *    schemas:
 *      City:
 *        type: object
 *        required:
 *          - country_id
 *          - city_name_ar
 *          - city_name_en
 *        properties:
 *          country_id:
 *            type: string
 *          city_name_ar:
 *            type: string
 *          city_name_en:
 *            type: string
 *        example:
 *           country_id: 5ea676283ca1d30b6822d0c3
 *           city_name_ar: abc xyz
 *           city_name_en: 5ea676283ca1d30b6822d0c3
 *      CityResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: city added
 */

//4. Contact
/**
 * @swagger
 *  components:
 *    schemas:
 *      Contact:
 *        type: object
 *        required:
 *          - about_us
 *          - address_ar
 *          - address_en
 *          - phone
 *          - mobile
 *          - email
 *          - instagram
 *          - twitter
 *          - facebook
 *          - website
 *        properties:
 *          about_us:
 *            type: string
 *          address_ar:
 *            type: string
 *          address_en:
 *            type: string
 *          phone:
 *            type: string
 *          mobile:
 *            type: string
 *          email:
 *            type: string
 *          instagram:
 *            type: string
 *          twitter:
 *            type: string
 *          facebook:
 *            type: string
 *          website:
 *            type: string
 *        example:
 *          about_us: abc
 *          address_ar: abc
 *          address_en: abc
 *          phone: 2345679852
 *          mobile: 2345679852
 *          email: abc@example.com
 *          instagram: link
 *          twitter: link
 *          facebook: link
 *          website: link
 *      ContactResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: contact added
 */

//5. WeekDay
/**
 * @swagger
 *  components:
 *    schemas:
 *      WeekDay:
 *        type: object
 *        required:
 *          - day_name_ar
 *          - day_name_en
 *          - nikname
 *        properties:
 *          day_name_ar:
 *            type: string
 *          day_name_en:
 *            type: string
 *          nikname:
 *            type: string
 *        example:
 *          day_name_ar: abc xyz
 *          day_name_en: abc xyz
 *          nikname: abc
 *      WeekDayResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: week day added
 */

//6. Region
/**
 * @swagger
 *  components:
 *    schemas:
 *      Region:
 *        type: object
 *        required:
 *          - city_id
 *          - country_id
 *          - region_name_ar
 *          - region_name_en
 *        properties:
 *          city_id:
 *            type: string
 *          country_id:
 *            type: string
 *          region_name_ar:
 *            type: string
 *          region_name_en:
 *            type: string
 *        example:
 *           city_id: 5ea676283ca1d30b6822d0c3
 *           country_id: 5ea676283ca1d30b6822d0c3
 *           region_name_ar: abc
 *           region_name_en: abc
 *      RegionResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: region added
 */

//8. Setting
/**
 * @swagger
 *  components:
 *    schemas:
 *      Setting:
 *        type: object
 *        required:
 *          - hospital_id
 *          - accept_reservation
 *          - cancel_reservation_period
 *          - discount
 *          - discount_percentage
 *        properties:
 *          hospital_id:
 *            type: string
 *          accept_reservation:
 *            type: number
 *          cancel_reservation_period:
 *            type: number
 *          discount:
 *            type: number
 *          discount_percentage:
 *            type: number
 *        example:
 *           hospital_id: 5ea676283ca1d30b6822d0c3
 *           accept_reservation: 1
 *           cancel_reservation_period: 0
 *           discount: 10
 *           discount_percentage: 0
 *      SettingResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: setting added
 */

//9. WebView
/**
 * @swagger
 *  components:
 *    schemas:
 *      WebView:
 *        type: object
 *        required:
 *          - link
 *        properties:
 *          link:
 *            type: string
 *        example:
 *          link: link
 *      WebViewResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: web-view added
 */

//10. TemplateMessages
/**
 * @swagger
 *  components:
 *    schemas:
 *      TemplateMessages:
 *        type: object
 *        required:
 *          - title
 *          - subject_ar
 *          - subject_en
 *          - message_ar
 *          - message_en
 *          - sender
 *        properties:
 *          title:
 *            type: string
 *          subject_ar:
 *            type: string
 *          subject_en:
 *            type: string
 *          message_ar:
 *            type: string
 *          message_en:
 *            type: string
 *          sender:
 *            type: string
 *        example:
 *           title: abc xyz
 *           subject_ar: abc xyz
 *           subject_en: abc xyz
 *           message_ar: abc xyz
 *           message_en: abc xyz
 *           sender: abc xyz
 *      TemplateMessagesResponse:
 *        type: object
 *        required:
 *          - status
 *          - message
 *        properties:
 *          status:
 *            type: boolean
 *          message:
 *            type: string
 *        example:
 *           status: true
 *           message: template messages added
 */

 //11. Notification
 /**
  * @swagger
  *  components:
  *    schemas:
  *      Notification:
  *        type: object
  *        required:
  *          - client_id
  *          - doctor_id
  *          - template_id
  *          - message_ar
  *          - message_en
  *          - type
  *          - status
  *        properties:
  *          client_id:
  *            type: string
  *          doctor_id:
  *            type: string
  *          template_id:
  *            type: string
  *          message_ar:
  *            type: string
  *          message_en:
  *            type: string
  *          type:
  *            type: string
  *          status:
  *            type: number
  *        example:
  *          client_id: 5ea676283ca1d30b6822d0c3
  *          doctor_id: 5ea676283ca1d30b6822d0c3
  *          template_id: 5ea676283ca1d30b6822d0c3
  *          message_ar: abc
  *          message_en: abc
  *          type: type
  *          status: 1
  *      NotificationResponse:
  *        type: object
  *        required:
  *          - status
  *          - message
  *        properties:
  *          status:
  *            type: boolean
  *          message:
  *            type: string
  *        example:
  *           status: true
  *           message: notification added
  */
